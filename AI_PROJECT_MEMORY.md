# AI Project Memory — PROJECT ANVIL

Last strategic grounding: 2026-08-21.

Accepted scientific truth remains through **ANVIL-10 / TORQUE-PATCH-REBIND**. **Epoch I is closed. No ANVIL-11 is active.** Live Git overrides this memory.

## Authority / project self-model

Repository: `Jozzpoly/PROJECT-ANVIL-Machine-Matter-Physical-Fabric-Laboratory`.

Authority order:
1. live Git/code + executable evidence;
2. direct Owner validation where genuinely required;
3. `.anvil/project-state.json` + `docs/CURRENT_HANDOFF.md` verified against live Git;
4. this memory + canonical docs;
5. conversation/donor history only as leads.

ANVIL may be treated as a third governance actor alongside Owner and Orchestrator. This is an operational metaphor, not literal consciousness.

- **Owner** — purpose, values, subjective acceptance, explicit vision changes.
- **Orchestrator** — investigation, falsification, planning, implementation, continuity.
- **Project self-model** — accepted truth, negative evidence, boundaries, active uncertainty, contradictions and next question.

Contradictions against accepted evidence/boundaries must be surfaced and consciously reclassified, never silently executed.

## Durable project identity

ANVIL investigates persistent authored Physical Fabric / Machine Matter meaning compiling into disposable runtime representations. Runtime Box3D bodies/joints/actions are interpretations, not construction identity. Cubic cells and `cellId@face` remain a laboratory dialect, not final ontology.

W1 / PR #22 is closed-unmerged negative product evidence after Owner verdict **NO VALUE SIGNAL — PRESENTATION / INSTRUMENT FAILURE**. Do not revive its gate/dashboard form.

P04 / PR #23 is closed-unmerged boundary-proof evidence. P05 / PR #24 is closed-unmerged technology-proof evidence selecting Vite + TypeScript, React sparse editor shell, imperative Three.js/WebGL2 presentation and existing Box3D/ANVIL runtime.

P06 attempt #1 / PR #26 is **INVALID / INTERRUPTED / CLOSED UNMERGED**. Its dashboard-heavy generated mockup is rejected and is not a design reference.

## Studio north star

**ANVIL Studio is one long-lived, world-first interactive 3D laboratory for Physical Fabric.**

`CREATE MATTER → GIVE IT LOCAL MEANING → COMPOSE → SIMULATE → OBSERVE → MODIFY → DISCOVER`

Grounded product loop:

`BUILD → GIVE LOCAL MEANING → RUN → OBSERVE → PAUSE / TRACE → STOP → MODIFY → RUN AGAIN`

Shortest constitution:

> **I build persistent matter. I give physical meaning to places on it. Studio continuously determines whether that authored composition can currently be realized. RUN creates a fresh disposable physical realization. I observe it, pause it and trace why it behaved that way. STOP discards the realization, not my construction. If I exceed ANVIL's earned capability, my authored intent survives and Studio tells me honestly that the composition is not yet supported.**

## P06 earned product contract so far

### C5A / Product Behavior — PASS
Canonical: `docs/studio/P06-C5A-PRODUCT-BEHAVIOR.md`.

- BUILD edits what persists;
- RUN creates fresh disposable runtime;
- PAUSE preserves runtime for STEP/TRACE, not arbitrary source edits;
- STOP disposes runtime, not construction;
- local meaning belongs to places on matter;
- `READY / INVALID / UNSUPPORTED / REQUIRES BUILD / RUNTIME FAULT` remain distinct;
- `UNSUPPORTED != INVALID`;
- authored operations: `TARGET → PREVIEW/DRAFT → COMMIT → RECOMPILE/RECLASSIFY → SPATIAL FEEDBACK`;
- accepted CUT remains a qualified topology intervention, not generic free editing.

### C5B / Product Interaction — PASS
Canonical: `docs/studio/P06-C5B-PRODUCT-INTERACTION.md`.

> **Matter first. Context second. Explanation on demand. Engineering internals last.**

> **Local truth close to matter; global state at workspace boundary; investigation only on request.**

BUILD/RUN/PAUSE/TRACE/STOP remain one workspace. RUN quiets UI. STOP must not fake reverse physics. Runtime manifestations are related to but not identical with authored marks. Instrumentation is question-driven and temporary. Save/Load preserves authored workspace, never runtime physics. View navigation is presentation-only. Generic Move/Copy/Duplicate/Multi-select are outside first envelope.

### P06.7A / Product Surface Architecture — PASS
Canonical: `docs/studio/P06-7A-PRODUCT-SURFACE-ARCHITECTURE.md`.

Selected:

> **WORLD CANVAS + PERIPHERAL ISLANDS**

Surfaces:

1. full-window **World Canvas**;
2. upper-left **Workspace Dock**;
3. left-edge **Intent Rail** — Select/Matter/Meaning/Inspect;
4. lower-center **Simulation Dock**;
5. local **Context/Draft Pod**;
6. conditional right-edge **Investigation Drawer**.

No permanent full-height inspector, hierarchy/outliner, timeline, dashboard, runtime entity browser, material-definition lab, environment editor or Generic Entity/Component inspector is earned.

Material surface assigns existing authored materials only. Simulation profile UI is contextual. First run stays in the same world with `Empty / Editable Starter`. Desktop reference `1440×900`; minimum first-envelope target `1024×640` CSS px, still requiring empirical validation.

### P06.7B / Visual Language — PASS
Canonical: `docs/studio/P06-7B-VISUAL-LANGUAGE.md`.

Selected v0 direction:

> **QUIET PHYSICAL WORKSHOP**

Visual constitution:

> **Matter should feel touchable. Meaning should look deliberately authored onto places. Runtime should become alive only when physics is alive. Chrome should behave like a quiet precision instrument. Deeper truth should appear as temporary explanation, not permanent spectacle.**

Governing grammar:

- shape = capability/kind;
- stroke/material treatment = authored/runtime lifetime;
- color reinforces but never solely carries semantics;
- motion only for real action/change/transition.

World / matter:

- neutral mid-dark `#202327–#262A2E`, not black void;
- calm workshop/studio lighting;
- authored material `displayColor` remains meaningful;
- matte tangible matter, subtle bevel/contact grounding, no permanent wireframe;
- grid is an orientation aid and may quiet in RUN.

Chrome / type:

- graphite `#1C1F23`;
- no glassmorphism/holographic panels/nested SaaS cards;
- spacing `4 / 8 / 12 / 16 / 24 px`, radius ~`8 px`, controls ~`32–36 px`;
- IBM Plex Sans normal UI; IBM Plex Mono only numeric/technical reveal;
- geometric stroke icons ~`18 px`.

Semantic families:

- Bearing `#4BC7C1` + ring/axis;
- Torque `#F2A65A` + patch/arrow;
- INVALID `#E56A6A` + broken/hatched form;
- UNSUPPORTED `#B7A0D8` + dashed/open frontier form;
- REQUIRES BUILD `#D7BC68` + lifetime transition cue;
- READY quiet;
- selection/focus uses two-tone high-contrast outline to survive arbitrary material colors.

Authored marks are solid/matte/stable. Runtime manifestations retain capability family but use distinct treatment and motion only when actual runtime behavior is active. TRACE uses temporary neutral ghosts/cross-highlights. STOP uses a brief fading final-runtime ghost, never reverse-physics motion.

Approx motion targets: hover `0–80 ms`, Context/Draft Pod `100–140 ms`, drawer `160–200 ms`, BUILD→RUN `120–180 ms`, STOP ghost `280–360 ms`, explicit Focus/Reframe camera `180–260 ms`.

Explicit anti-patterns: black void + neon HUD, holographic/glass panes, permanent glowing/rainbow overlays, generic dark SaaS cards, all-terminal UI, dramatic game lighting obscuring matter, or any visual concept that rewrites P06.7A surfaces for fashion.

## Accepted boundaries

Do not renegotiate without real contradiction/new evidence:

- `authored != compiled != runtime != render`;
- `render entity != authored entity != runtime entity`;
- runtime is disposable;
- React does not own/mirror hot 60 Hz runtime state;
- Three scene objects are presentation-only;
- selection crosses renderer/application as sparse semantic refs/events;
- generic authored edits may rebuild/reset runtime unless continuity is qualified;
- `UNSUPPORTED != INVALID`;
- `cellId@face` is current lab dialect, not final ontology;
- runtime manifestation is not authored source itself;
- no permanent inspector/hierarchy/timeline/dashboard is earned for first envelope.

## Empirical obligations for later implementation/fidelity

Not proven by design:

- `1024×640` density/readability;
- arbitrary material-color clashes;
- extreme view angle/zoom handle legibility;
- real keyboard focus/contrast;
- outline/ghost browser/GPU behavior;
- font/icon optical quality;
- hot Three overlay performance while React remains sparse.

## Current P06 method

Every mini-stage uses `PLAN → REALIZATION → RED TEAM → VERDICT`.

Before Product Design acceptance:

- ImageGen/mockups do not invent product structure;
- Build Web Apps does not invent product behavior/information architecture;
- no Studio implementation;
- styling expresses but cannot rewrite earned structure.

Build Web Apps returns after accepted Product Design as implementation/fidelity discipline.

## Next selected stage — P06.8 / Complete Product Contract + adversarial red-team

Combine C5A + C5B + P06.7A + P06.7B as one falsifiable product. Walk complete representative journeys and states, attack W1/corrupted-P06/CAD/SaaS regressions, source/runtime identity confusion, hidden-only interaction, instrumentation clutter and any decision silently deferred to implementation.

Classify every remaining issue as blocker / implementation-fidelity / research boundary / explicit Owner choice.

Issue **PRODUCT DESIGN ACCEPTED** only if no fundamental product decision remains for implementation.

Then proceed to **C6 / IMPLEMENTATION READY → Build Web Apps implementation/fidelity**.

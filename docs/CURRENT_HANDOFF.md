# PROJECT ANVIL — Current Handoff

Status: **ANVIL-10 ACCEPTED / EPOCH I CLOSED / W1 CLOSED NEGATIVE PRODUCT EVIDENCE / STUDIO P06 SELECTED**

Live Git and executable evidence override this checkpoint if they differ.

## Start here

1. Resolve live `main` and open PRs before writing.
2. Accepted scientific truth remains through **ANVIL-10 / TORQUE-PATCH-REBIND** only.
3. Accepted material checkpoint: `ffde8c0babdd473454b3e769cb10fd31537a0c70`; evidence grounding: `ba7ce2dc67c6f7aa936e20a0294d9cc12208a549`.
4. Epoch I is closed. No ANVIL-11 is active or implied by the Studio work.
5. PR #22 / Workbench W1 is **closed unmerged** after owner verdict **NO VALUE SIGNAL — PRESENTATION / INSTRUMENT FAILURE**. Treat it as negative integration/product evidence and donor code only.
6. PR #23 / P04 Studio boundary proof is **closed unmerged / PASS** for the first active-bearing Studio envelope.
7. PR #24 / P05 technology proof is **closed unmerged / TECHNOLOGY PROVEN — PASS** and selected the first Studio engineering direction: existing Vite + TypeScript, React sparse editor shell, imperative Three.js presentation controller, WebGL2 first, existing Box3D/ANVIL runtime.
8. The next selected stage is **P06 / ANVIL Studio Product Design**. Do not implement Studio product code before its complete design/state contract is accepted.

## Scientific boundary

ANVIL-00…10 remain the complete accepted scientific stack. Studio preparation, renderer qualification, UI/product design and integration do not create ANVIL-11 or promote generic FUNCTION, Control, Signal, Surface, Entity, Relation, runtime or representation-independence semantics.

Runtime physics remains disposable. Cubic cells and `cellId@face` remain the current laboratory authoring dialect, not final Machine Matter ontology.

## W1 negative evidence

PR #22 proved that the frozen Workbench B0 path could be assembled technically and qualified for owner inspection. The owner-facing result was nevertheless too raw, unreadable and workflow-heavy to demonstrate useful Workbench product value.

The correct interpretation is:

- technical qualification ≠ product value;
- the failure belongs to presentation/instrument/integration form, not Physical Fabric science;
- do not merge or polish the Workbench AUTHORED/RUNTIME/BOTH gate UI into the product;
- UX and visualization are research instrumentation;
- progress should be measured by real owner agency in one persistent world.

## Studio north star

**ANVIL Studio is a long-lived interactive 3D laboratory for Physical Fabric.**

Core loop:

`CREATE MATTER → GIVE IT LOCAL MEANING → COMPOSE → SIMULATE → OBSERVE → MODIFY → DISCOVER`

The world is the primary surface. The product must not regress into separate experiment pages, evidence dashboards or owner-gate choreography.

## P04/P05 engineering locks

The first implementation envelope is bounded by:

- `render entity != authored entity != runtime entity`;
- authored / compiled / runtime / presentation remain separate;
- React owns sparse editor/application state, not 60 Hz runtime state;
- Three owns presentation/camera/picking/overlays/gizmos and receives runtime observations directly through a thin controller;
- renderer selection crosses into application/UI state as sparse semantic references/events;
- gizmos manipulate preview/draft state until an explicit authoring command commits a persistent edit;
- generic source edits may reset/rebuild runtime; continuity is used only where separately qualified;
- WebGL2 is the first backend; WebGPU is not a launch gate;
- no `react-three-fiber`, generic state manager, generic `FabricRuntime` or other abstraction is selected without a demonstrated need.

These are engineering/product constraints, not new scientific claims.

## P06 exact task

P06 must freeze the complete product experience before implementation:

1. complete primary Studio screen, not a hero/header fragment;
2. viewport-first information hierarchy and chrome density;
3. direct selection/manipulation grammar for matter and local face meaning;
4. explicit EDIT / RUN / PAUSE / unsupported / invalid / rebuild/trace states;
5. authored-meaning, runtime and provenance overlay grammar;
6. inspector behavior and progressive technical reveal;
7. visual language, typography, colors, spacing, icons, motion and selection states;
8. representative responsive/minimum-desktop behavior;
9. exact core journeys: understand, change, give meaning, simulate, intervene and explore;
10. implementation inventory and design-system extraction only after the visual concept is accepted.

Use @Build Web Apps as a design/verification discipline. Generate complete visual concepts first. Do not write Studio product code while the concept is under review.

## P06 design filters

Prefer:

- one dominant 3D world;
- direct manipulation;
- calm, legible, technically precise chrome;
- contextual/adaptive tools near the current selection;
- minimal persistent HUD with deeper detail revealed on demand;
- obvious physical consequences shown in the world before textual explanation;
- playful creative immediacy without sacrificing technical truth.

Reject:

- dashboard-first layouts;
- dense evidence/log walls;
- nested card grids and fake metrics;
- decorative sci-fi HUD styling that competes with the world;
- permanent mode chrome that explains the process more loudly than the matter;
- any UI representation that collapses authored, compiled and runtime identity.

## Stop conditions

Stop/reclassify rather than silently widening P06 if the design requires:

- a new physical law or scientific capability;
- multi-bearing/multi-capability runtime behavior that P04 explicitly left unsupported;
- arbitrary CUT continuity;
- final representation-independent locality semantics;
- Foundation promotion or a generic ontology merely for UI convenience.

## Next action

Create and review complete P06 visual concepts against this contract. After owner acceptance, extract the frozen design system, state catalogue and component/interaction inventory. Only then prepare C6 / implementation-ready build plan.

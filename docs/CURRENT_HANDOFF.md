# PROJECT ANVIL — Current Handoff

Status: **ANVIL-07 / ELASTIC-SEAM PROMOTED — POST-PROMOTION GROUNDED**

This is a short takeover pointer, not a science archive. Live Git and executable evidence override this file if they differ.

## Start here

1. Resolve live `main` from GitHub before writing.
2. Treat accepted material checkpoint `62dcc651f73dc3f228109d3d8922afd534b75950` as a claim to verify against live Git.
3. Read `docs/experiments/ANVIL-07-ELASTIC-SEAM-EVIDENCE.md` for the exact ANVIL-07 evidence chain.
4. Verify Ready run `32188493917` belongs to source head `a34a769e4892bb5a3c117389af7c2bba47866623` and synthetic merge `c206a008458e2a244bf65663f0c11705a1d1c948` if promotion provenance matters.
5. Do not reconstruct ANVIL-00…06 unless live Git exposes a contradiction that requires it.
6. No experiment is currently active. Do not treat the ranked next falsifier as already frozen or implemented.

## Accepted truth

Accepted material capability stack is now through **ANVIL-07 / ELASTIC-SEAM**.

Material promotion identity:

```text
promoted source head  a34a769e4892bb5a3c117389af7c2bba47866623
Ready run              32188493917
Ready synthetic merge  c206a008458e2a244bf65663f0c11705a1d1c948
Ready/merge tree       d9833ba637f11c11ca7f01f67924b128191e1ada
actual material merge  62dcc651f73dc3f228109d3d8922afd534b75950
evidence log commit    97ab4d72475ae3d15ae79a496cb7bdb6689c952f
```

The Ready candidate ran core + packaged launcher + existing Chromium regression. Core was 42/42 PASS, Chromium 19/19 PASS, launcher self-test PASS. The actual merge tree is identical to the Ready synthetic tree.

## ANVIL-07 accepted interpretation

For the frozen seven-cell one-dimensional fixture, one persistent local authored compliant seam can replace rigid adjacency at a source interface, compile into disposable runtime representation, deform under load and restore after unload while remaining causally distinct from RIGID and FREE controls.

The accepted claim remains bounded. It does **not** establish generic deformable matter, generic Bond/Relation ontology, resolution-independent constitutive compliance, arbitrary shear/bending/rotation, plasticity, damage, contact-loaded compliance, compliant REBIND, control, signals, power, or a reason to promote `ElasticSeam` into foundation.

Exact metrics, hardening readback and artifact identities live in `docs/experiments/ANVIL-07-ELASTIC-SEAM-EVIDENCE.md`.

## Current strategic frontier

There is no active ANVIL-08 yet. The post-ANVIL-07 Research Compass ranking is:

1. **COMPLIANCE-RESOLUTION** — strongest next falsifier. Challenge whether the same intended physical compliant interface retains honest macroscopic meaning across different source resolutions/interface patch counts without per-voxel retuning, while authored and runtime resolution remain allowed to differ.
2. **ACTIVATE** — leading runner-up. Introduce transient control over already-earned persistent function semantics and force a composition checkpoint.
3. compliant **REBIND** — defer until the simpler compliance representation/scaling assumption is understood.

Before implementation of the next experiment, freeze a new preflight. Do not silently reuse ANVIL-07's discrete `N/m` and `N*s/m` seam coefficients as resolution-independent material fields.

After at most one deliberate compliance-resolution challenge, the next strategic step should force composition of earned semantics unless new evidence materially changes the frontier.

## Exact next action

Run a bounded post-promotion meso/macro preparation pass for **COMPLIANCE-RESOLUTION**:

- restate the strongest assumption to falsify;
- define coarse/fine authored representations of the same physical interface;
- derive candidate area-normalized/local semantics without committing them to foundation;
- design a naive fixed-per-patch control that would expose representation dependence;
- decide what may be aggregated into a different-resolution disposable runtime relation;
- freeze gates before executable results;
- stop before implementation if the physical meaning cannot be stated independently of voxel count.

This is research preparation, not permission to implement or promote a new schema yet.

## Do not do now

- do not reopen or enrich ANVIL-07;
- do not change its frozen C0 gates or coefficients;
- do not promote `ElasticSeam` or a generic compliant binding into `src/foundation`;
- do not assume `normalStiffnessNPerM` / `normalDampingNsPerM` are resolution-independent material properties;
- do not start ACTIVATE merely because it is easier than the leading falsifier;
- do not start compliant REBIND before scaling/representation meaning is understood;
- do not add a large speculative roadmap; keep one executable next falsifier and a small ranked horizon.

## Stable project documents

- `AGENTS.md` — agent rules, truth hierarchy and implementation review cadence.
- `docs/EXPERIMENT_PROTOCOL.md` — per-experiment evidence lifecycle.
- `docs/RESEARCH_COMPASS.md` — macro anti-drift / frontier validation method.
- `docs/FOUNDATION.md` — only already-earned reusable foundation boundaries.
- `AI_PROJECT_MEMORY.md` — concise accepted-state and strategic pointer.
- `.anvil/project-state.json` — machine-readable current checkpoint claim.
